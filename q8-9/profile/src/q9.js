
// Write a method on the component that takes in one argument for name and returns a string with the letters reversed.

// import React from 'react';

// const ReverseTool = ({name}) => {
//     const reverseName = (name) => {
//             return name.split('').reverse().join('');
//         }

//     return (
//         <>
//             {name && (
//                 <div>Reversed name: {reverseName(name)}</div>
//             )}
//         </>
//     )
// }
// // export default ReverseTool;
// module.exports = ReverseTool;
